import styled from 'styled-components'

const Button = styled.button`
  display: flex;
  align-items: center;
  padding: 0.75em 1em;
  font-size: 1.25em;
  font-weight: 700;
  margin: 2em auto;
  background: tomato;
  color: white;
  border: none;
  border-radius: 3px;
`

export default Button
