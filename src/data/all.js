const data = require('./all-possible-arr.json')

function getIteration() {

}

data.map((item, i) => {
  let sum = item.numbers.forEach((el, index) => {
    return console.log(el)
  })
  //console.log(sum)
})
